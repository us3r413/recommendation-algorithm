# AI 職缺推薦演算法 — 簡報講稿

> 組別：夏威夷披薩｜組員：林哲緯、鄭仲軒、戴煦
> 建議分工：開場＋架構（一人）、第一階段（一人）、第二＋三階段（一人）、結論一起收尾。可自行調整。
> ⚠️ 標記為「【補充】」的段落，是因為原投影片文字不完整、我依上下文與 GraphRAG 邏輯補上的，請對照你們實際做法確認。

---

## 投影片 1：標題頁

大家好，我們這組是「夏威夷披薩」，組員是林哲緯、鄭仲軒、戴煦。

今天要跟大家分享的題目是「AI 職缺推薦演算法」。簡單來說，這是一個**基於 AWS 雲端技術，並結合生成式 AI 打造的智慧求職推薦系統**。

我們想解決的問題是：求職者在找工作時，輸入的關鍵字常常很口語、很模糊，甚至有錯字或縮寫，傳統的關鍵字比對很難精準抓到他真正想要的職缺。所以我們設計了一套流程，讓系統能「聽懂」使用者的需求，再從大量職缺中找出最合適的推薦給他。

---

## 投影片 2 & 3：我們的架構

先帶大家看一下整個系統的架構，這樣後面分階段講的時候會比較好對照。

從這張圖來看，流程是這樣的：使用者透過瀏覽器或 App 送出一個查詢請求（API Request），這個請求會進到我們部署在 **AWS 雲端**上的系統。整個系統核心分成三個階段：

- **第一階段 Query Parser**：負責理解使用者的查詢。這裡我們會呼叫 AWS Bedrock 上的 **Claude 3.5 Sonnet**，把口語化的查詢轉成系統看得懂的關鍵字。
- **第二階段 Retriever**：用 **DuckDB** 從大約**一百萬筆**的職缺資料（CSV）裡，快速撈出符合條件的職缺。
- **第三階段 Ranker**：負責排序，決定哪些職缺要排在前面；另外還有一個可選的 **Graph Search** 模組，會事先建好職缺之間的關聯圖來輔助排序。

最後系統把排好序的結果透過 API Response 回傳給使用者。

接下來我們就一個階段一個階段詳細說明。

---

## 投影片 4 & 5：第一階段 — Query Parsing

第一階段是 Query Parsing，也就是「查詢解析」。

這個階段的目標，是把使用者輸入的 **query 和 filter，轉換成一組 tag（標籤）**，也就是系統後面可以拿來搜尋的關鍵字。

那具體做了哪些事呢？

第一，**對模糊的條件做 semantic expansion**，語意擴展。使用者講的東西通常很籠統，我們會把它展開成一組更具體、更完整的關鍵字。

第二，我們**使用 AWS Bedrock 上的線上 LLM** 來做這件事，因為這種語意理解的任務，交給大型語言模型的效果會比寫死的規則好很多。

第三，我們也做了**錯別字矯正**，避免使用者打錯字就搜不到東西。

第四，我們**額外建了一個縮寫字典**，先把常見縮寫還原，減少 LLM 看不懂縮寫、判斷錯誤的機率。

講到這裡可能有點抽象，直接看兩個例子就很清楚：

- 使用者輸入「**便利商店 打工**」，LLM 會展開成：`7-11`、`全家`、`萊爾富`、`OK 超商`、`便利商店`、`兼職`。它自己補上了各家便利商店的品牌名，還把「打工」對應到「兼職」。
- 又例如輸入「**coffee shop barista**」，會展開成：`星巴克`、`路易莎`、`cama`、`咖啡`、`咖啡師`、`吧台`。連英文都能對應到台灣本地的咖啡品牌跟中文職務名稱。

所以這一階段的重點，就是**讓系統真正「聽懂」使用者要什麼**，把模糊的自然語言變成一組精準、可以拿去搜尋的關鍵字。

---

## 投影片 6 & 7：第二階段 — Retriever（Data Retrieval）

有了tag之後，進入第二階段：Retriever，也就是資料檢索。

這一階段做的事情，是**把前面產生的 tag 變成篩選條件**。

我們會判斷每個 tag 的性質：**可以直接當成篩選欄位的 tag**，例如地區、職務類別、工作型態這類結構化的條件，就直接拿去做資料庫的篩選；**其他比較自由、無法對應到固定欄位的 tag，則拿來做關鍵字比對篩選**。

這樣做的好處是，先用結構化條件把資料量大幅縮小，再用關鍵字做細部比對，效率會高很多。

那為什麼選 DuckDB？因為我們的資料集大約有一百萬筆，如果每次查詢都要慢慢掃過整份 CSV 會很慢。**DuckDB 是一個很適合做這種本地端分析查詢的資料庫，能大幅加快我們取用與篩選資料的速度**，讓整個檢索在很短的時間內完成。

> 【補充】原投影片這頁最後有一個空項目，如果你們還有想補的內容（例如篩選後大概剩多少筆、或是有沒有做去重、快取），可以在這裡順帶提一下。

---

## 投影片 8 & 9：第三階段 — Ranking（Ranker）

第三階段是 Ranking，排序。經過前兩階段，我們已經篩出一批符合條件的職缺，但這些職缺要用什麼順序呈現給使用者，就是這一階段要決定的。

我們的**排序方法**主要有兩種：

第一種是**熱門程度排序**：越熱門的職缺，排越前面。這是最直覺、也最穩定的基準。

第二種是 **Graph 相關性排序**，也就是我們用到 GraphRAG 的地方。

**當我們知道**一個使用者之前應徵過某個特定的職缺，我們就能去推測：像他這樣的人，接下來還會去應徵什麼樣的職缺？因為在資料裡，很多人的應徵路徑是有規律的——會投 A 職缺的人，往往也會投 B、C 職缺。我們把這種「常常被同一群人一起應徵」的職缺在圖上連起來，就能沿著這張圖，幫使用者找出他可能也會喜歡、但自己不一定想得到的職缺。

我們用 networkx 建了一張使用者—職缺互動圖， 44.1 萬個節點、320 萬條邊，其中user-job interactions的權重是應徵 3 分、瀏覽 1 分。另外，性質、所需技能、地區相似的職缺和行為相似的使用者會在圖中會有邊連接 上面實作了兩跳協同過濾：透過「其他行為相似的使用者」來幫忙推薦。

先講匿名使用者的情況，也就是圖上這條主流程。它一共走兩跳：第一跳，我們拿使用者這次輸入的查詢條件，去找出「過去也搜尋過同樣條件的其他使用者」；第二跳，再去看「這些使用者實際互動、應徵過哪些職缺」。把這些職缺收集起來，就是我們要推薦的結果。背後的假設很直覺——會搜尋類似條件的人，通常興趣也相近，那別人投過的職缺，很可能就是你也會喜歡的。

那如果是已登入、有紀錄的使用者呢？這時我們改用這位使用者自己過去的應徵紀錄，去找出「行為跟他相似的一群使用者」，接著一樣看這群人互動過的職缺，推薦給他。因為有個人化的歷史資料，這種推薦會比匿名的更貼近他本人。

不過使用 Graph 也有代價：**搜尋會變慢**，因為在圖上做關聯搜尋的計算量比單純排序大很多。所以我們把它設計成一個**可選（Optional）**的模組——在需要更精準、更個人化推薦時才啟用，一般情況下用熱門程度排序就足夠，兼顧速度與品質。

---

## 投影片 10 & 11：結果與討論（Conclusion）

最後跟大家分享我們怎麼評估這套系統的效果。

我們寫了一支**評分程式**來衡量推薦的準確度。

我們使用6/5之前的資料進行訓練

**評分標準是這樣設計的**：我們看使用者在 **6/6 到 6/7** 這段期間，實際「查詢」並且真的「投遞履歷」的行為，用 **hit rate** 來計算 Accuracy。

白話講就是——**如果我們推薦的職缺，剛好就是使用者後來真的去投的職缺，那就算命中一次**。命中的比例越高，代表我們的推薦越貼近使用者真正的需求，系統就越成功。用真實的投遞行為當作答案，會比主觀評分更客觀、更有說服力。

> 【補充】原投影片這頁最後有一個空項目。如果你們有跑出實際的數字（例如 hit rate 幾 %、開 Graph 跟沒開 Graph 的差異），這裡就是放結果數據、講亮點的最佳時機，會讓結論更有力。

不過，我們也想誠實跟大家說明這個指標本身的限制，因為資料集其實存在一個結構性的偏誤。

大多數使用者只會看搜尋結果的第一頁，會往後翻的人並不多。所以越排在前面的職缺，被看到、被應徵的機會就越高。

一個職缺曝光多，就會累積更多應徵；應徵多了，下次又更容易被排在前面。這樣就形成一個自我強化的回饋迴圈——排在前面的職缺會一直排在前面，也就是所謂「富者越富」。換句話說，資料裡的投遞行為，本身就已經被排序機制影響過了。

我們的 hit rate 是建立在「使用者實際投遞」的行為上，而使用者會投什麼，又受到他「看到什麼」影響。所以一個只靠熱門程度排序的系統，很可能拿到虛高的分數——它推薦的熱門職缺，本來就是使用者會看到、也會去投的那些。這不代表它推薦得好，也有可能是它重現了既有的偏誤。

所以我們的結論是：這個 hit rate 比較適合拿來做「相對比較」的參考，例如比較「有開 Graph 個人化排序」跟「只用熱門排序」之間的差異，而不適合當成系統絕對好壞的鐵證。這也正好凸顯了我們做 Graph 排序的用意——它就是想跳脫熱門迴圈，去挖出使用者可能會喜歡、但不一定排在前面的職缺。
---

## 投影片 12：Thank You / Q&A  

以上就是我們「夏威夷披薩」的分享。

我們用三個階段——先用 AWS Bedrock 的 LLM 聽懂使用者的查詢、再用 DuckDB 快速檢索、最後結合熱門度與 GraphRAG 做個人化排序——打造出這套智慧求職推薦系統。

謝謝大家的聆聽，以下開放 Q&A，有任何問題都歡迎提出。

---

### 附：可能被問到的 Q&A（提前準備）

- **為什麼用 LLM 而不是寫規則做關鍵字擴展？** → 口語、縮寫、錯字的組合太多，規則寫不完；LLM 有語意理解與世界知識，泛化能力強。
- **為什麼選 DuckDB 而不是傳統資料庫？** → 資料是 CSV、量級約百萬筆、以分析型查詢為主，DuckDB 免架設、讀 CSV 快，很適合這個場景。
- **Graph 排序既然慢，為什麼還要用？** → 它能抓到關鍵字比對不到的「隱性關聯」，提升個人化與命中率；所以設計成可選，用在需要精準推薦時。
- **只用 6/6–6/7 兩天的資料評估，會不會太少？** → 可以誠實回答這是目前的驗證範圍，未來會擴大時間區間與樣本數讓結果更穩定。
